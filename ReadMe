This project uses graph search methods like Ford Fulckerson algorithm and the
idea  of MinCut and MaxFlow to find out if and which teams are mathematically
eliminated in Baseball games. It calculates the max a team can achieve and
determines if they still have a chance at winning.

Performance: O(Exf) E: edges, f: maximum flow 
